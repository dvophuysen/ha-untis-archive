# Schul-Cockpit – Dashboard für zwei Kinder gegen das UNTIS-Archive.
#
# Drei Hauptseiten, ein gemeinsamer Kind-Umschalter:
#   1. Übersicht       – Monatskalender mit Stundenplan, Fehltagen und
#                        Klassenarbeiten. Klick auf einen Termin öffnet
#                        die Untis-typische Detailansicht (Lehrstoff,
#                        Lehrer/Originallehrer, Raum, Vertretungs-Info).
#   2. Fächer          – Pro Fach eine chronologische Lehrstoff-Historie
#                        mit Vertretungs-Markierung.
#   3. Krankheits-Ausfälle – Pro Krankheits-Periode der verpasste Stoff,
#                        gruppiert nach Fach.
#
# Voraussetzungen (siehe docs/dashboard.md):
#   • HACS-Karten: atomic-calendar-revive, mushroom.
#   • Helper input_select.schul_cockpit_kind mit Optionen „Anna" / „Ben".
#   • Helper input_select.schul_cockpit_fach (Optionen werden per
#     Automation aus dem Fach-Verlauf-Sensor des aktuellen Kindes
#     dynamisch gefüllt).
#
# Entity-IDs gehen davon aus, dass die Untis-Config-Entries die Titel
# „Anna" und „Ben" tragen (Device-Slugs `untis_archive_anna` /
# `untis_archive_ben`). Bei anderen Anzeigenamen entsprechend ersetzen.

title: Schul-Cockpit
views:
  # ────────────────────────────────────────────────────────────────────
  # 1. ÜBERSICHT – Monatskalender mit allem drin
  # ────────────────────────────────────────────────────────────────────
  - title: Übersicht
    path: uebersicht
    icon: mdi:calendar-month
    cards:
      # ── Zentraler Kind-Umschalter (in jeder View identisch) ───────────
      - type: horizontal-stack
        cards:
          - type: custom:mushroom-template-card
            primary: Anna
            secondary: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Anna') %}● aktiv{% else %}wechseln{% endif %}
            icon: mdi:account-school
            icon_color: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Anna') %}blue{% else %}disabled{% endif %}
            fill_container: true
            tap_action:
              action: call-service
              service: input_select.select_option
              service_data:
                entity_id: input_select.schul_cockpit_kind
                option: Anna
          - type: custom:mushroom-template-card
            primary: Ben
            secondary: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Ben') %}● aktiv{% else %}wechseln{% endif %}
            icon: mdi:account-school
            icon_color: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Ben') %}blue{% else %}disabled{% endif %}
            fill_container: true
            tap_action:
              action: call-service
              service: input_select.select_option
              service_data:
                entity_id: input_select.schul_cockpit_kind
                option: Ben

      # Stundenplan + Fehltage + Klassenarbeiten in einem Monatskalender.
      # atomic-calendar-revive akzeptiert mehrere Calendar-Entities und
      # rendert ihre Events farblich getrennt.
      - type: conditional
        conditions:
          - condition: state
            entity: input_select.schul_cockpit_kind
            state: Anna
        card:
          type: custom:atomic-calendar-revive
          name: Stundenplan – Anna
          enableModeChange: true
          defaultMode: Calendar
          showMonth: true
          showWeekNumber: true
          firstDayOfWeek: 1
          showLocation: true
          showDescription: true
          showFullDayProgress: true
          showCurrentEventLine: true
          dimFinishedEvents: true
          finishedEventOpacity: 0.5
          entities:
            - entity: calendar.untis_archive_anna_stundenplan
              color: "#1976d2"
            - entity: calendar.untis_archive_anna_ereignisse
              color: "#e53935"

      - type: conditional
        conditions:
          - condition: state
            entity: input_select.schul_cockpit_kind
            state: Ben
        card:
          type: custom:atomic-calendar-revive
          name: Stundenplan – Ben
          enableModeChange: true
          defaultMode: Calendar
          showMonth: true
          showWeekNumber: true
          firstDayOfWeek: 1
          showLocation: true
          showDescription: true
          showFullDayProgress: true
          showCurrentEventLine: true
          dimFinishedEvents: true
          finishedEventOpacity: 0.5
          entities:
            - entity: calendar.untis_archive_ben_stundenplan
              color: "#1976d2"
            - entity: calendar.untis_archive_ben_ereignisse
              color: "#e53935"

      - type: markdown
        content: |
          **Legende** &nbsp;·&nbsp; ❌ Ausfall &nbsp;·&nbsp; ↺ Vertretung &nbsp;·&nbsp;
          🤒 versäumt (Krankheit) &nbsp;·&nbsp; 📝 Klassenarbeit
          (rote Markierung) &nbsp;·&nbsp; ganztägige rote Balken = Fehltage.
          Termin anklicken → Detailansicht mit Lehrstoff, Lehrer und Raum.

  # ────────────────────────────────────────────────────────────────────
  # 2. FÄCHER – Stoff-Historie pro Fach
  # ────────────────────────────────────────────────────────────────────
  - title: Fächer
    path: faecher
    icon: mdi:bookshelf
    cards:
      # ── Zentraler Kind-Umschalter (in jeder View identisch) ───────────
      - type: horizontal-stack
        cards:
          - type: custom:mushroom-template-card
            primary: Anna
            secondary: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Anna') %}● aktiv{% else %}wechseln{% endif %}
            icon: mdi:account-school
            icon_color: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Anna') %}blue{% else %}disabled{% endif %}
            fill_container: true
            tap_action:
              action: call-service
              service: input_select.select_option
              service_data:
                entity_id: input_select.schul_cockpit_kind
                option: Anna
          - type: custom:mushroom-template-card
            primary: Ben
            secondary: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Ben') %}● aktiv{% else %}wechseln{% endif %}
            icon: mdi:account-school
            icon_color: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Ben') %}blue{% else %}disabled{% endif %}
            fill_container: true
            tap_action:
              action: call-service
              service: input_select.select_option
              service_data:
                entity_id: input_select.schul_cockpit_kind
                option: Ben

      - type: custom:mushroom-template-card
        primary: "Fach wählen"
        secondary: "{{ states('input_select.schul_cockpit_fach') }}"
        icon: mdi:book-open-variant
        icon_color: indigo
        tap_action:
          action: more-info
          entity: input_select.schul_cockpit_fach

      - type: conditional
        conditions:
          - condition: state
            entity: input_select.schul_cockpit_kind
            state: Anna
        card:
          type: markdown
          content: |
            {% set fach = states('input_select.schul_cockpit_fach') %}
            {% set subjects = state_attr('sensor.untis_archive_anna_fach_verlauf', 'subjects') or {} %}
            {% set items = subjects.get(fach, []) %}
            {% if items | count == 0 %}
            _Wähle oben ein Fach._

            Verfügbar: {{ (state_attr('sensor.untis_archive_anna_fach_verlauf', 'subject_list') or []) | join(' · ') }}
            {% else %}
            ## {{ fach }} – Lehrstoff-Historie (Anna)
            _Letzter Stoff zuerst. ↺ Vertretung, ❌ Ausfall, 🤒 versäumt._

            {% for it in items -%}
            {% set hh = (it.start // 100) | string %}{% set mm = '%02d' % (it.start % 100) %}
            {% set marker =
              ('❌ Ausfall · ' if it.code == 'cancelled'
               else ('🤒 versäumt · ' if it.was_absent
               else ('↺ Vertretung · ' if it.code == 'irregular' else ''))) %}
            ### {{ it.date }} · {{ hh }}:{{ mm }}
            {{ marker }}{{ it.topic or '_(kein Lehrstoff erfasst)_' }}
            {%- if it.teacher %}
            _Lehrer: {{ it.teacher }}{% if it.room %} · Raum {{ it.room }}{% endif %}_
            {%- endif %}

            {% endfor %}{% endif %}

      - type: conditional
        conditions:
          - condition: state
            entity: input_select.schul_cockpit_kind
            state: Ben
        card:
          type: markdown
          content: |
            {% set fach = states('input_select.schul_cockpit_fach') %}
            {% set subjects = state_attr('sensor.untis_archive_ben_fach_verlauf', 'subjects') or {} %}
            {% set items = subjects.get(fach, []) %}
            {% if items | count == 0 %}
            _Wähle oben ein Fach._

            Verfügbar: {{ (state_attr('sensor.untis_archive_ben_fach_verlauf', 'subject_list') or []) | join(' · ') }}
            {% else %}
            ## {{ fach }} – Lehrstoff-Historie (Ben)
            _Letzter Stoff zuerst. ↺ Vertretung, ❌ Ausfall, 🤒 versäumt._

            {% for it in items -%}
            {% set hh = (it.start // 100) | string %}{% set mm = '%02d' % (it.start % 100) %}
            {% set marker =
              ('❌ Ausfall · ' if it.code == 'cancelled'
               else ('🤒 versäumt · ' if it.was_absent
               else ('↺ Vertretung · ' if it.code == 'irregular' else ''))) %}
            ### {{ it.date }} · {{ hh }}:{{ mm }}
            {{ marker }}{{ it.topic or '_(kein Lehrstoff erfasst)_' }}
            {%- if it.teacher %}
            _Lehrer: {{ it.teacher }}{% if it.room %} · Raum {{ it.room }}{% endif %}_
            {%- endif %}

            {% endfor %}{% endif %}

  # ────────────────────────────────────────────────────────────────────
  # 3. KRANKHEITS-AUSFÄLLE – pro Periode der verpasste Stoff
  # ────────────────────────────────────────────────────────────────────
  - title: Krankheits-Ausfälle
    path: krankheit
    icon: mdi:bed
    cards:
      # ── Zentraler Kind-Umschalter (in jeder View identisch) ───────────
      - type: horizontal-stack
        cards:
          - type: custom:mushroom-template-card
            primary: Anna
            secondary: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Anna') %}● aktiv{% else %}wechseln{% endif %}
            icon: mdi:account-school
            icon_color: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Anna') %}blue{% else %}disabled{% endif %}
            fill_container: true
            tap_action:
              action: call-service
              service: input_select.select_option
              service_data:
                entity_id: input_select.schul_cockpit_kind
                option: Anna
          - type: custom:mushroom-template-card
            primary: Ben
            secondary: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Ben') %}● aktiv{% else %}wechseln{% endif %}
            icon: mdi:account-school
            icon_color: >-
              {% if is_state('input_select.schul_cockpit_kind', 'Ben') %}blue{% else %}disabled{% endif %}
            fill_container: true
            tap_action:
              action: call-service
              service: input_select.select_option
              service_data:
                entity_id: input_select.schul_cockpit_kind
                option: Ben

      - type: conditional
        conditions:
          - condition: state
            entity: input_select.schul_cockpit_kind
            state: Anna
        card:
          type: markdown
          content: |
            {% set periods = state_attr('sensor.untis_archive_anna_krankheitsperioden', 'periods') or [] %}
            {% if periods | count == 0 %}
            ## Keine Krankheits-Ausfälle im laufenden Schuljahr 🎉
            {% else %}
            ## Krankheits-Ausfälle – Anna
            _{{ periods | count }} Periode{% if periods | count != 1 %}n{% endif %} im Schuljahr. Pro Periode der versäumte Stoff, gruppiert nach Fach — das ist die Liste, die das Kind sich von den Klassenkameraden besorgen muss._

            {% for p in periods %}
            ---
            ### 🤒 {{ p.start_date }}{% if p.end_date and p.end_date != p.start_date %} – {{ p.end_date }}{% endif %}
            {% if p.is_excused %}✅ entschuldigt{% else %}⚠️ unentschuldigt{% endif %}{% if p.reason %} · {{ p.reason }}{% endif %} · **{{ p.lessons_count }} Stunden** versäumt

            {% if p.subjects | count == 0 %}
            _Keine Lehrstoff-Einträge für diese Periode (möglicherweise nichts dokumentiert oder Stunden nicht im Archiv-Fenster)._
            {% else %}
            {% for fach in p.subjects.keys() | sort %}
            **{{ fach }}**
            {% for it in p.subjects[fach] -%}
            {% set hh = (it.start // 100) | string %}{% set mm = '%02d' % (it.start % 100) %}
            - {{ it.date }} · {{ hh }}:{{ mm }} – {{ it.topic or '_(kein Lehrstoff erfasst – bei Klassenkameraden erfragen)_' }}{% if it.teacher %} _(· {{ it.teacher }})_{% endif %}
            {% endfor %}

            {% endfor %}
            {% endif %}
            {% endfor %}{% endif %}

      - type: conditional
        conditions:
          - condition: state
            entity: input_select.schul_cockpit_kind
            state: Ben
        card:
          type: markdown
          content: |
            {% set periods = state_attr('sensor.untis_archive_ben_krankheitsperioden', 'periods') or [] %}
            {% if periods | count == 0 %}
            ## Keine Krankheits-Ausfälle im laufenden Schuljahr 🎉
            {% else %}
            ## Krankheits-Ausfälle – Ben
            _{{ periods | count }} Periode{% if periods | count != 1 %}n{% endif %} im Schuljahr. Pro Periode der versäumte Stoff, gruppiert nach Fach — das ist die Liste, die das Kind sich von den Klassenkameraden besorgen muss._

            {% for p in periods %}
            ---
            ### 🤒 {{ p.start_date }}{% if p.end_date and p.end_date != p.start_date %} – {{ p.end_date }}{% endif %}
            {% if p.is_excused %}✅ entschuldigt{% else %}⚠️ unentschuldigt{% endif %}{% if p.reason %} · {{ p.reason }}{% endif %} · **{{ p.lessons_count }} Stunden** versäumt

            {% if p.subjects | count == 0 %}
            _Keine Lehrstoff-Einträge für diese Periode (möglicherweise nichts dokumentiert oder Stunden nicht im Archiv-Fenster)._
            {% else %}
            {% for fach in p.subjects.keys() | sort %}
            **{{ fach }}**
            {% for it in p.subjects[fach] -%}
            {% set hh = (it.start // 100) | string %}{% set mm = '%02d' % (it.start % 100) %}
            - {{ it.date }} · {{ hh }}:{{ mm }} – {{ it.topic or '_(kein Lehrstoff erfasst – bei Klassenkameraden erfragen)_' }}{% if it.teacher %} _(· {{ it.teacher }})_{% endif %}
            {% endfor %}

            {% endfor %}
            {% endif %}
            {% endfor %}{% endif %}
